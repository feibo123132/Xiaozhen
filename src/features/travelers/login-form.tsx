'use client';
import { useRouter } from 'next/navigation';
import { useState, type FormEvent } from 'react';
export function TravelerLoginForm() { const router = useRouter(); const [message, setMessage] = useState(''); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const response = await fetch('/api/traveler/session', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ publicId: data.get('publicId'), password: data.get('password') }) }); const result = await response.json(); if (!response.ok) { setMessage(result.error); return; } router.push('/me'); router.refresh(); } return <form className="admin-form" onSubmit={submit}><label>旅人编号<input name="publicId" placeholder="traveler-..." required /></label><label>密码<input name="password" type="password" required /></label><p role="alert">{message}</p><button className="ink-button">回到我的旅人</button></form>; }
