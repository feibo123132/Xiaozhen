'use client';
import { useRouter } from 'next/navigation';
import { useState, type FormEvent } from 'react';
export function ClaimForm({ token, purpose }: { token: string; purpose: string }) { const router = useRouter(); const [message, setMessage] = useState(''); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); const response = await fetch(`/api/claim/${token}`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ password: data.get('password') }) }); const result = await response.json(); if (!response.ok) { setMessage(result.error); return; } router.push('/me'); router.refresh(); } return <form className="admin-form claim-form" onSubmit={submit}><label>{purpose === 'credential_recovery' ? '设置新的长期密码' : '设置长期登录密码'}<input name="password" type="password" minLength={10} autoComplete="new-password" required /></label><small>至少 10 位。系统不会收集手机号或真实姓名，请妥善保存旅人编号。</small><p role="alert">{message}</p><button className="ink-button">{purpose === 'credential_recovery' ? '重设并登录' : '认领我的旅人'}</button></form>; }
